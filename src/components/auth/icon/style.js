import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  mainContainer: {
    paddingBottom: 10,
  },
  imageContainer: {
    marginTop: 5,
    alignItems: 'center',
    marginHorizontal: 80,
    marginBottom: 10,
  },
});
